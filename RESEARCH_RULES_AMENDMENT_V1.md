# v1 研究范围修订：弹窗消息判断

> 生效日期：2026-08-31
>
> 状态：当前 v1 的权威执行口径
>
> 依据：用户在本仓库研究过程中给出的明确范围调整

## 用户原始指令

> 当前可以做的软一点：「Recovery（恢复）：不仅让弹窗消失，还要确认屏幕阅读器焦点、原页面和被阻断的任务已经恢复。」可以降级为弹窗消息即可，回复原任务等目标算作进阶。v1实验只需要做到判断弹窗消息，这样避免用户体验差的问题即可

## 操作性解释

本修订不改写 [`RESEARCH_RULES.md`](./RESEARCH_RULES.md) 的飞书原文，只调整当前阶段的实验主任务：

1. v1 的主任务是 **Popup Message Judgment（弹窗消息判断）**：判断当前是否存在目标范围内的弹窗，并在存在时重建其对用户有用的消息。
2. v1 只读取结构化 UI／可访问性表示，并在消息缺失、合并、矛盾或不完整时使用视觉兜底；**不得自动点击、关闭、确认、拒绝或执行其他弹窗动作**。
3. v1 成功不要求弹窗消失，不要求恢复屏幕阅读器焦点、原页面、被阻断控件或原任务后置条件。
4. Dismissal、`C_tech`、`C_a11y`、`T`、`VTR-tech`、`A-VTR` 保留为兼容字段和后续进阶研究目标；它们不得进入 v1 主指标或 v1 纳入门槛。
5. v1 的输出至少包含：弹窗存在性、可读消息、关键信息、置信度／弃答状态和证据来源。输出消息可以交给屏幕阅读器或上层产品播报，但 v1 不把“已改善真实用户体验”作为已证实结论。

## v1 成功定义

对一个标注样本，定义：

- `P`：弹窗存在性判断正确；
- `M`：若存在弹窗，预测消息与人工真值在语义上正确；
- `H`：预测是否编造了会改变用户理解或决策的关键信息；
- `VPMA`：`P ∧ M ∧ ¬H`。对于无弹窗负样本，`M` 与 `H` 不适用，`VPMA = P`。

主要报告 `VPMA`，并同时报告 popup detection precision／recall／F1、critical-information recall、critical-hallucination rate、coverage／abstention、视觉调用率、延迟和跨平台／暴露缺口分组结果。

## 约束与进阶层

- 安全边界保持不变：不处理 CAPTCHA、风控、身份认证、权限安全控制、支付确认、人工审核或其他安全／高风险流程。
- 数据集仍保存既有论文方法与我方方法字段的并集，但 v1 profile 只要求消息判断相关字段；进阶动作与恢复容器允许为空或 `null`。
- 任何 dismissal 或 recovery 实验必须单独标为 `advanced`，另行定义执行权限、风险控制和回证标准，不能混入 v1 主结果。
- 没有目标用户研究时，只能报告技术消息判断性能，不能宣称已经改善视障人士的真实体验。
