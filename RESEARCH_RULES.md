<title>背景-贡献</title>

# 背景

提升无障碍模式下，视障人士使用移动端设备出现弹窗，长时间无法消除，极大影响体验的问题

# 问题

移动端设备在无障碍模式下，读取可访问性树时，无法100%消除弹窗问题，导致体验受到极大影响

# 原因

ui元素无法全部暴露在可访问性树，如 系统框架会合并节点；系统过滤、限制；开发者缺陷（未暴露在可访问性树中）；各平台对外展示button和text不一致，导致无法统一用程序获取，等等

# 做法

参考ppt的诸多方法，可以用【读取ui结构化表示+视觉兜底】

# 实验+人工数据集

基于已有的调研文献（必须在边界内）的实验，寻找他们实验寻找ui结构化表达的item，取并集，再加上我们方法中的item，一起作为一个数据的所有属性。再随机话 × N

## Item

需要对齐ios/android等主流系统在暴露给可访问性树时，有哪些内容可以让弹窗取消

# 边界

移动端+正常弹窗关闭，不解决封控和人工检测等弹窗，且至少包含：识别弹窗+消除弹窗+回证

# 我们工作的侧重点

第一个提出这个背景和问题的工作

第一个评估弹窗问题的公开数据集

解决方法＋实验指标比其他方法好
